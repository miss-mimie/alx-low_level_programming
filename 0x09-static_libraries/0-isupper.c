#include "main.h"
/**
 * _isupper - check for uppercase letters
 * @c:  char to check 
 *
 * Return: if uppercase return 1, else return o
 */
int _isupper(int c)
{
	if ( c >= 'A' && c <= 'z' )
		return (1);
}
